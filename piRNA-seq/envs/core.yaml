# Legacy: piRNA-seq rules no longer use this conda env.
# Pinned tools live in containers/pipeline.def (Apptainer). run_workflow.sh omits --use-conda for piRNA-seq.
name: pirna_core
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - fastqc
  - cutadapt
  - bowtie
  - samtools
  - deeptools
